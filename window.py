import random
import tkinter as tk
from PIL import Image, ImageTk
from tkinter import messagebox


root = tk.Tk()
root.title("score-db")
root.resizable(False, False) #視窗可縮放設定
w,h = (800,600)
root.geometry(str(w)+'x'+str(h)) #視窗大小


root.mainloop()